## Packages
firebase | Authentication (Google Sign-In)
framer-motion | Parallax animations and transitions
lucide-react | Icons for UI
wouter | Routing
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
Firebase configuration requires environment variables.
Parallax effects will use Framer Motion `useScroll` and `useTransform`.
Chat interface needs to handle loading states for AI responses.
Mocking Firebase initialization if env vars are missing to prevent runtime crash during development preview.
